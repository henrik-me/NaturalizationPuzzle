name: External review policy signal

on:
  pull_request_review:
    types: [submitted, edited, dismissed]

permissions: {}

jobs:
  signal:
    runs-on: ubuntu-24.04
    timeout-minutes: 1
    steps:
      - name: Complete unprivileged review signal
        run: echo "Trusted workflow_run reconciliation requested."

